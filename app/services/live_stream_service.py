def start_live_stream(stream_url: str):
    # Code to start live streaming
    print(f"Live stream started at {stream_url}")

def stop_live_stream(stream_url: str):
    # Code to stop live streaming
    print(f"Live stream stopped at {stream_url}")
